diff --git a/prisma/schema.prisma b/prisma/schema.prisma
index 9528a2b..0000000 100644
--- a/prisma/schema.prisma
+++ b/prisma/schema.prisma
@@ -84,12 +84,13 @@ model Customer {
   id             String   @id @default(auto()) @map("_id") @db.ObjectId
   organizationId String?  @db.ObjectId
   name           String
-  email          String   @unique
+  email          String
   tickets        Ticket[]
   createdAt      DateTime @default(now())
   updatedAt      DateTime @updatedAt
 
+  @@unique([organizationId, email])
   @@index([organizationId])
 }
@@ -393,10 +394,11 @@ model Tag {
   id             String   @id @default(auto()) @map("_id") @db.ObjectId
   organizationId String?  @db.ObjectId
-  name           String   @unique
+  name           String
   color          String   @default("slate")
   createdAt      DateTime @default(now())
 
+  @@unique([organizationId, name])
   @@index([organizationId])
 }
@@ -414,12 +416,13 @@ model SlaPolicy {
   id                    String   @id @default(auto()) @map("_id") @db.ObjectId
   organizationId        String?  @db.ObjectId
   name                  String
-  priority              String   @unique
+  priority              String
   firstResponseMinutes  Int
   resolutionMinutes     Int
   createdAt             DateTime @default(now())
   updatedAt             DateTime @updatedAt
 
+  @@unique([organizationId, priority])
   @@index([organizationId])
 }
@@ -445,13 +448,14 @@ model EmailConfig {
   imapPort       Int      @default(993)
   imapUser       String
   imapPass       String
-  fromAddress    String   @unique
+  fromAddress    String
   fromName       String   @default("Support")
   isActive       Boolean  @default(false)
   isDefault      Boolean  @default(false)
   createdAt      DateTime @default(now())
   updatedAt      DateTime @updatedAt
 
+  @@unique([organizationId, fromAddress])
   @@index([organizationId, isActive])
 }
diff --git a/prisma/schema-tenancy.test.ts b/prisma/schema-tenancy.test.ts
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/prisma/schema-tenancy.test.ts
@@ -0,0 +1,39 @@
+import { readFileSync } from "node:fs";
+import { join } from "node:path";
+import { describe, expect, it } from "vitest";
+
+const schema = readFileSync(join(process.cwd(), "prisma", "schema.prisma"), "utf8");
+
+function modelBlock(modelName: string) {
+  const match = schema.match(new RegExp(`model ${modelName} \\{([\\s\\S]*?)\\n\\}`, "m"));
+
+  if (!match) {
+    throw new Error(`Missing Prisma model: ${modelName}`);
+  }
+
+  return match[1];
+}
+
+describe("tenant-owned unique constraints", () => {
+  it.each([
+    ["Customer", "email"],
+    ["Tag", "name"],
+    ["SlaPolicy", "priority"],
+    ["EmailConfig", "fromAddress"],
+  ])("scopes %s.%s by organization", (modelName, fieldName) => {
+    const block = modelBlock(modelName);
+
+    expect(block).toContain(`@@unique([organizationId, ${fieldName}])`);
+    expect(block).not.toMatch(new RegExp(`\\b${fieldName}\\s+String\\s+@unique\\b`));
+  });
+});
diff --git a/docs/implementation-status.md b/docs/implementation-status.md
index 080b297..0000000 100644
--- a/docs/implementation-status.md
+++ b/docs/implementation-status.md
@@ -22,11 +22,11 @@ Development continues on `feat/saas-foundation` in draft PR #3. It supersedes PR
 - Health and readiness endpoints
 - Security response headers
 - CI validation for Prisma, TypeScript, ESLint, Prettier, tests, and production builds
 - Complete API inventory and repository-wide authorization audit
+- Tenant-safe composite uniqueness for customers, tags, SLA policies, and mailboxes
 
 ## Active Migration Work
 
-- Apply tenant-safe composite uniqueness to customers, tags, SLA policies, and mailboxes
 - Complete tenant scoping for analytics, notifications, email templates/logs, CSAT, bulk operations, users, SLA, polling, and inbound email paths
 - Resolve tests affected by Clerk-aware and tenant-aware service signatures
 - Remove temporary migration workflows after their one-time commits complete
